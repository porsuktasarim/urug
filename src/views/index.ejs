<%- include('partials/header', { title: null, t }) %>

  <div class="text-center py-5">
    <h1 class="display-5"><%= appName %></h1>
    <p class="text-muted">Proje iskeleti çalışıyor. Veritabanı bağlantısı aktif.</p>
    <div class="d-flex gap-2 justify-content-center flex-wrap">
      <a href="/aileler" class="btn btn-primary"><%= t('family.title') %></a>
      <a href="/kisiler" class="btn btn-primary"><%= t('person.title') %></a>
      <% if (typeof isGlobalAdmin !== 'undefined' && isGlobalAdmin) { %>
        <a href="/ayarlar" class="btn btn-outline-secondary"><i class="bi bi-gear me-1"></i>Ayarlar</a>
      <% } %>
    </div>
  </div>

<%- include('partials/footer', { t }) %>
